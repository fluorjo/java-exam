public class gns2dowhile {
        public static void main(String[]args) {
        int cnt=0;
        do{
            cnt+=1;
            System.out.println(cnt + "회 반복");
            
        } while (cnt<=4); //cnt가 4보다 작거나 같을 동안 반복. cnt가 5가 되면 멈춤.
        }
}
